Marriott Inventory System - Workspace notes

Files restored by assistant:
- `inventory.py` : simple CSV-backed inventory helper
- `inventory_app.py` : tiny GUI demo for inventory helper

How to run the main app:

```powershell
cd "c:\Users\My plus computers\Desktop\Marriott _Sys"
python Main.py
```

If the GUI closes immediately, capture logs:

```powershell
python Main.py > run.log 2>&1
notepad run.log
```
